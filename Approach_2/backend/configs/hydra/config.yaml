defaults:
  - model: attention_mil
  - _self_

dataset:
  cohort: "default"
  tile_px: 256
  tile_um: 256
  
feature_extraction:
  extractor: "resnet50"
  device: "cuda"

training:
  outdir: "/data/slideflow/mil_models"
  val_fraction: 0.2
  epochs: 10
  batch_size: 32
