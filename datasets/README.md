# Datasets

This folder is reserved for local datasets and raw archives used by the MSI workflows.

Expected local layout:

```text
datasets/
  CRC-VAL-HE-7K/
  raw_archives/
```

These contents are intentionally ignored by Git because they are too large for a normal GitHub repository push.
